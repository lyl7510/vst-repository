/*
import Vst from "../index";
import VstForm, {Iform, Irule} from "../../comps/form";

export interface IformPageState {
    myForm?: Iform;
    rules?: Irule;
}


interface IformPage<P, S extends IformPageState> {

    myForm: VstForm;

    onChange(name: string, e: Vst.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>): void;

    showMessage(name: string, message: string): void;

    onSubmit(): void;

    submit(): void;

    cancle?(): void;
}

export default IformPage;*/
